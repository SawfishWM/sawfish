# Makefile.in -- input for the capplet directory's Makefile
# $Id: Makefile.in,v 1.11 2000/04/08 10:10:25 john Exp $
#
# Copyright (C) 1999 John Harper <john@dcs.warwick.ac.uk>
#
# This file is part of sawmill.
#
# sawmill is free software; you can redistribute it and/or modify it
# under the terms of the GNU General Public License as published by
# the Free Software Foundation; either version 2, or (at your option)
# any later version.
#
# sawmill is distributed in the hope that it will be useful, but
# WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU General Public License for more details.
#
# You should have received a copy of the GNU General Public License
# along with sawmill; see the file COPYING.  If not, write to
# the Free Software Foundation, 675 Mass Ave, Cambridge, MA 02139, USA.

include ../Makedefs

top_builddir=..
top_srcdir=@top_srcdir@
srcdir=@srcdir@
VPATH=@top_srcdir@/src:.

override CFLAGS += $(GNOME_CC_CFLAGS)
override LIBS += $(GNOME_CC_LIBS)

G_CC_INFIX := share/control-center
G_MENU_INFIX := share/gnome/apps/Settings

all : sawfish-capplet

sawfish-capplet : sawmill-capplet.c ../src/libclient.o
	$(CC) $(CPPFLAGS) $(CFLAGS) $(LDFLAGS) -o $@ $^ $(LIBS)

install : all installdirs
	for p in sawfish-capplet; do \
	  $(INSTALL_PROGRAM) $$p $(DESTDIR)${bindir}; \
	done
	-set -e; for d in $(prefix) $(GNOMEDIRS); do \
	  if [ -d $(DESTDIR)$$d/$(G_CC_INFIX) ]; then \
	    rm -rf $(DESTDIR)$$d/$(G_CC_INFIX)/Sawfish; \
	    rm -rf $(DESTDIR)$$d/$(G_CC_INFIX)/Sawmill; \
	    mkdir $(DESTDIR)$$d/$(G_CC_INFIX)/Sawfish; \
	    $(INSTALL_DATA) desktop-entries/.directory $(DESTDIR)$$d/$(G_CC_INFIX)/Sawfish/; \
	    for f in desktop-entries/*.desktop; do \
	      $(INSTALL_DATA) $$f $(DESTDIR)$$d/$(G_CC_INFIX)/Sawfish/; \
	    done; \
	    break; \
	  fi \
	done
	-set -e; for d in $(prefix) $(GNOMEDIRS); do \
	  if [ -d $(DESTDIR)$$d/$(G_MENU_INFIX) ]; then \
	    rm -rf $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawfish; \
	    rm -rf $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawmill; \
	    mkdir $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawfish; \
	    $(INSTALL_DATA) desktop-entries/.directory $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawfish/; \
	    for f in desktop-entries/*.desktop; do \
	      $(INSTALL_DATA) $$f $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawfish/; \
	    done; \
	    break; \
	  fi \
	done

uninstall :
	for p in sawfish-capplet; do \
	  rm -f $(DESTDIR)${bindir}/$$p; \
	done
	-set -e; for d in $(prefix) $(GNOMEDIRS); do \
	  if [ -d $(DESTDIR)$$d/$(G_CC_INFIX)/Sawfish ]; then \
	    rm -rf $(DESTDIR)$$d/$(G_CC_INFIX)/Sawfish; \
	  fi; \
	  if [ -d $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawfish ]; then \
	    rm -rf $(DESTDIR)$$d/$(G_MENU_INFIX)/Sawfish; \
	  fi \
	done

installdirs : $(top_srcdir)/mkinstalldirs
	$(SHELL) $(top_srcdir)/mkinstalldirs $(DESTDIR)$(bindir)

clean :
	rm -f *~ *.o *.lo *.la build.h core

realclean : clean
	rm -f .*.d sawfish-capplet Makefile

include .sawmill-capplet.d
