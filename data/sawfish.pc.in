prefix=@prefix@
exec_prefix=@exec_prefix@
libdir=@libdir@
includedir=@includedir@

imageloader=@imageloader@

Name: sawfish
Description: The Sawfish X11 Window Manager
Version: @version@
Requires: librep >= @REP_MIN_VER@ rep-gtk >= @REP_GTK_MIN_VER@ gtk+-2.0 >= @GTK_MIN_VER@ @IMAGE_PC@

Cflags: -I${includedir}
Libs: -L${libdir} -lX11
