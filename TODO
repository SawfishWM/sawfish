-*- indented-text -*-

todo:

  ! fix resize

  ! fix workspace problems (all done now?)

  ! fix move rubber-band munging title bars

  + regrab keys when keymaps change

  + pixmap cursors / cursor recolouring

  + menu backend program

    Use the rep-gtk, read-write s-expressions to display a menu and
    return the selection. Leave scope for other user-interface elements
    (graphical configuration!?)

  + iconification (?)

  + more seamless ``theming''

    Possibly define a `load-theme' function and lay down some interface
    requirements..?

  + export more Imlib functions

    (colour modifiers, the bevel function in Imlib 1.9.5+)

  - GNOME compliance

    At least for the workspaces (so the pager works)
